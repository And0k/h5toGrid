in C:\Work\Python\AB_SIO_RAS\h5toGrid\scripts\downloading there programs for downloading data. Find better developed parts of code inside files and organize all code to keep most functionality for all tasks, so at 1st analyze and think about better architecture then describe in new scripts/downloading/README.md. This README must provide a clear description of each Python script within the `scripts/downloading/` directory, explaining its purpose, the workflow it implements, how to execute it, and its expected output. Additionally, it will document the key utility functions found in `scripts/downloading/utils.py` or anywhere in same dir. 

Currently we in transition the configuration from Python variables in [`scripts/downloading/config.py`](scripts/downloading/config.py) to a hierarchical YAML structure using the `Hydra` module. Created a new `cfg` directory within `scripts/downloading/` and defined separate YAML files (e.g., `base.yaml`, `copernicus.yaml`, `ncep.yaml`) for different categories of settings, so after describing how all must work in readme.md, update scripts to implement all your improvements along with loading configurations from these YAML files if not yet.

After finishing improvements of code update README.md according to your obtained specific result
